// Package goalkeeper composes an ADK loop workflow that runs a worker agent
// followed by a validator agent in the same ADK invocation and session until
// validation passes or the configured iteration limit is reached.
package goalkeeper
